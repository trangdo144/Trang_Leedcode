/**
 * @param {number[]} numbers
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(numbers, target) {
    let left = 0;
    let right = target;
    while( left < right - 1){
        let mid = left + Math.trunc((right - left)/2);
        if()
    }
};